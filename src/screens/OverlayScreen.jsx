import React from 'react';

const OverlayScreen = () => {
  return (
    <div style={{ width: '100%', height: '100%', backgroundColor: '#222', display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
      <h1>Overlay Mode (Trace)</h1>
      {/* Camera Preview + Image Overlay would go here */}
    </div>
  );
};

export default OverlayScreen;
